
// Github issue #14 mentioned that this code would segfault

func recTill(n: Int) {
    if n <= 0 {
        return
    }

    recTill(n - 1)
}

// recTill(2131) // This does not segfault
// recTill(2132) // This does segfault

func recTill(n: Int) { if n <= 0 { return }; recTill(n - 1) }
func a(n: Int) { print(n); a(n + 1); }

// This example shows the usage of `me`
// With me, you can directly refer to the current object instead of the nearest scope

Test("object property").test("testarg")

class Test {
	local: String
	
	func test(local: String) {
		print("Should be 'testarg':")
		print(local)
		print("Should be 'object property':")
		print(me.local)
	}
}

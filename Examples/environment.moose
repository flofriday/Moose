// Here we test the environments, and print them.
func add(a: Int, b: Int) > Int {
    unused = "Just to play with it"
    mut sum = 0

    if true {
        evil = "You shouldn't be able to see mee in the env"
    }

    if true {
        sum =  a + b
        inner = "I am useless, but you can see mee (^.^)"
        environment()
    }

    return sum
}

add(12, 34)
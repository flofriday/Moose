// Calculate the first 20th fibonacci numbers recursively
func fib(n: Int) > Int {
    if n <= 1 {
        return n
    }

    return fib(n - 1) + fib(n - 2)
}

for i in range(20) {
    println(String(i) + ": " + String(fib(i)))
}

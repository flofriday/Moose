// Calculate the first 20th fibonacci numbers recursively
func fib(n: Int) > Int {
    if n <= 1 {
        return n
    }

    return fib(n - 1) + fib(n - 2)
}

for mut i = 0; i < 20; i = i + 1 {
    print(String(i) + ": " + String(fib(i)))
}
